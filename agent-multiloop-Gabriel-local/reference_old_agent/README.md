# Ancien agent multiloop — Code de référence

Ce dossier contient le code de l'agent multiloop **actuel** de Gabriel.

⚠️ Ce code sert de **référence** pour analyse. Le nouvel agent sera développé dans `/app/agent-multiloop-Gabriel-local/src/`.

## Instructions pour Gabriel

Chargez ici les fichiers de votre agent actuel, en respectant si possible
l'arborescence d'origine. Par exemple :

```
reference_old_agent/
├── main.py
├── agent/
│   ├── loop.py
│   ├── tools.py
│   └── ...
├── prompts/
├── config/
└── ...
```

Vous pouvez charger les fichiers :
- Soit en les collant directement dans le chat (un par un ou par groupe)
- Soit via l'éditeur VS Code une fois celui-ci accessible
- Soit en les attachant comme pièce jointe dans le chat

## Suivi des fichiers chargés

| Fichier | Date de chargement | Analysé |
|---------|--------------------|---------| 
| _(à compléter au fur et à mesure)_ | | |
